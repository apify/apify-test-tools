# apify-test-tools e2e fixture

A minimal consumer repo of `apify-test-tools`. The end-to-end harness (`../harness`) copies it to a
fresh branch of the sandbox repo on every run; nothing here runs from this directory. See
[`../README.md`](../README.md).
